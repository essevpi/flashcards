const styles = {
  modal:
    'absolute flex items-center justify-center w-full h-full inset-0 backdrop-blur-sm',
  modalContent: 'flex flex-col bg-red-900 rounded-lg p-4',
};

export default styles;
